export type Product = {
  id: string;
  title: string;
  price: number;        // PLN
  category: string;     // np. "electronics", "home", "sport"
  rating: number;       // 0-5
  image: string;        // placeholder URL / data URL
};
